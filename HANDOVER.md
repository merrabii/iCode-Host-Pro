# HANDOVER

## Read in order
README.md → PROJECT_CONTEXT.md → PROJECT_STATUS.md → DECISIONS.md → TASKS.md → CHANGELOG.md → MASTER_PROMPT.md → docs references.

## Before changing code
Determine current phase, actual implementation, proposed versus approved decisions, blockers and owner test requirements. If unclear, analyze rather than guess.

## Current state (Phase 10bis + 10bis.5 — GITHUB → COOLIFY DEPLOY, 2026-09-03, plan owner-approved « go », IMPLEMENTED + fully verified (unit 280/280 27 suites, e2e 149/149 19 suites, tsc API+web PASS, web build PASS 19 routes) — on top of Phase 11 (ADR-028, committed `c315369`, owner-validated). **Awaiting OWNER LIVE VALIDATION → commit + push. ADR-029 stays PROPOSED (owner decision — do NOT implement).**)
- **Phase 10bis.5 (2026-09-03, ADR-030 addendum) — « coller l'URL d'un dépôt » + détection auto — GitHub linking becomes OPTION**: `POST /api/client/deployments/detect {url}` → `GithubService.detectRepo` (**never throws**, best-effort) : `sanitizeGitUrl` (http(s) only, trim, fragment/query stripped, **private/reserved hosts refused — SSRF guard** : localhost, literal IPs, RFC1918/169.254) · github.com → **public GET without token** `/repos/{owner}/{repo}` (default_branch + language) + best-effort `/contents/Dockerfile` → `dockerfile` suggestion, else `suggestBuildPack(language)` (nixpacks default) ; 404/network → fallback `main`/nixpacks + `detail` · `Deployment` model + `repoUrl?`/`buildPack?`/`appName?` (migration 14 `20260903140253_init_deployment_url`) · `createGitApp` body `name: appName ?? serviceName`, `build_pack: buildPack ?? 'nixpacks'` · DTO « exactly one of repoFullName|repoUrl » (cross `@ValidateIf`), buildPack `@IsIn(BUILD_PACKS)` · `create` URL mode **skips** GitHub token/ownership, branch = dto ?? detected ?? main, buildPack = dto ?? suggested ?? nixpacks, appName = dto ?? service.name · web `/client` panel **2 tabs** (GitHub lié if `github.linked` + URL always available : URL → Détecter → editable **Build pack** + **Nom de l'app** → Deploy). **Verified 2026-09-03**: tsc api+web PASS, unit 280/280, e2e 149/149 (+4 clientB-without-GitHub: detect 201, deploy URL 201 DEPLOYING stored buildPack/appName + no decryptToken call, private/invalid URL 400, both modes 400; e2e fake `detectRepo` delegates to the REAL `sanitizeGitUrl` so the SSRF guard is exercised, zero network otherwise), web build PASS 19 routes (dev stopped + `.next` purged). **Live smoke (non-destructive, throwaway JWT, no real account touched)**: `GET /api/public/auth-config` → `deployEnabled:false` · `detect` without flag → **403** · flag toggled on temporarily (throwaway SecuritySetting row, restored after) → `detect` real URL `expressjs/express` → **201** {master, JavaScript, nixpacks} · private URL `192.168.1.10` → **400** (SSRF) · `gitlab.com/gitlab-org/gitlab` → **201** fallback main/nixpacks + detail. **ROOT CAUSES of « nothing changes on the client workspace » (configuration, NOT a code bug — owner must fix)**: (1) `deployEnabled` is OFF (no `SecuritySetting` row → default) so the panel is hidden on `/client` ; (2) **no ACTIVE `Service` exists** to deploy onto ; (3) the stored Coolify API token is **refused** by the real server (`403 « You are not allowed to access the API »` on `portal.arumdigital.com:8000`) → re-verify the panel on `/manager/serveurs` with a **ROOT/write** token. Full live create→deploy still awaiting owner validation.
- **Phase 10bis (2026-09-03, ADR-030, plan validated « go »)**: auto-detect GitHub repos (`GithubService` — `GET /api/client/github/repos` + `POST github/link-status`, store `githubTokenEnc` from Phase 10, decrypted on the fly, Bearer + `X-GitHub-Api-Version: 2022-11-28`) · `Deployment` model (migration 13 `20260903033537_init_deployment`, `DeploymentStatus PENDING/DEPLOYING/ACTIVE/FAILED`, `coolifyUuid` NEVER exposed to the API — ADR-021) · `PanelTransport` extended with `createGitApp` (`POST /applications/public` — **endpoint CONFIRMED against the real Coolify 4.1.2**: `/applications/git` 404s, fixed live), `deployApp`, `deploymentStatus` (best-effort, never rejects) · flow `POST /api/client/deployments` (guards: `deployEnabled` / repo owned / Service ACTIVE / server COOLIFY + `panelOk=true` → PENDING → createGitApp → deployApp → DEPLOYING, audit `deploy.create`; failure → FAILED + `deploy.failed` + 502; `GET /deployments/:id` **live-refreshes** when DEPLOYING, audit `deploy.status`) · web `/client` « Déploiements » panel (repo select w/ default branch, ACTIVE service select, Deploy, status badges, **8 s poll** while DEPLOYING) · **OPERATIONAL REQUIREMENT (observed live)**: Coolify needs an API **ROOT / write-scope** token to create an app (read-only token → 403 « Missing required permissions: write ») — expected Coolify behavior, not a code bug. Private repos via Coolify GitHub App = documented follow-up. **Live verification**: read path OK against real `portal.arumdigital.com:8000` (version 4.1.2, 424 ms, 5 apps, status parsing); write path uncovered 404 `/applications/git` then 403 write-permission → route fixed. **Flaky note**: 1 unit test `invitations.service.spec` (file NOT touched by 10bis, `expiresAt = Date.now()+1 s` fragile in long runs) failed 1×/2 full runs; passes 16/16 in isolation — optional hardening before next commit.
- **Audit & corrections (2026-09-03, owner directive: audit → technical corrections only → permissions matrix → traffic/quotas + ADR → DESIGN_HANDOVER → tests → final report; NO commit/push/10bis until explicit validation)**:
  - **3 technical fixes (no new features)**: (1) product delete now **409 ConflictException** when referenced by subscriptions (`products.service.remove` counts `Subscription` first; suggested alternative = DISABLED status) + unit spec; (2) client workspace catalog reads `listPublicProducts()` (DRAFT/DISABLED filtered — `GET /api/products` stays authenticated, core.e2e intact); (3) knowledge editor alert text aligned with reality (`/aide` is public).
  - **Knowledge base seeded** (`apps/api/prisma/seed-knowledge.ts` + `db:seed:knowledge`): idempotent create-if-missing by `[audience, slug]`, never overwrites admin edits, author = oldest ADMIN, all PUBLISHED. **34 articles** — 23 ADMIN (12 INFORMATIVE recaps Phase 1→11 from real CHANGELOG/DECISIONS, 4 TECHNICAL, 7 HOWTO) + 11 CLIENT (Premiers pas / Compte & sécurité / Support). Validated on real `icode-postgres`: 34 created, re-run → 0 (idempotence proven).
  - **4 docs created**: `docs/permissions-matrix.md` (real route→access matrix + strict knowledge separation CLIENT+PUBLISHED list-without-body + transversal rules) · `docs/traffic-quotas.md` (4-layer measure/quota/limit/suspend analysis — `quotaMaxAccounts` NOT enforced, manual `isActive` suspension only; **ADR-029 PROPOSED**, nothing implemented) · `docs/parcours-commande-publique.md` (end-to-end public order flow audit + documented gaps without invention) · `docs/design/DESIGN_HANDOVER.md` (entry point for the future design agent: 8 imperative rules, functional vs presentation file map, behaviors to preserve, out-of-scope list).
  - **e2e isolation root-cause fix**: 9 pre-existing failures (`security-settings` 2 + `oauth` 7) from cross-suite clobbering of the singleton `SecuritySetting` row on shared Postgres with parallel jest workers (each suite passes alone with `--runInBand`). Fix: `"maxWorkers": 1` in `apps/api/test/jest-e2e.json`. Full run now **132/132 (18 suites)** green.
  - **Verified 2026-09-03**: unit **224/224**, e2e **132/132**, tsc API+web PASS, `web build` PASS (18 routes, `.next` purged + dev stopped before build), knowledge seed run on real DB (34 created → idempotent re-run).
  - **ADR-029 PROPOSED** added to DECISIONS.md (traffic/quotas/suspension 4-layer architecture) — intentionally NOT in the APPROVED list. **Owner decision 2026-09-03: keep it PROPOSED, do NOT implement now.**
  - **OWNER-VALIDATED 2026-09-03 ✓ → committed + pushed.** Next: **Phase 10bis plan** — present, do NOT start until explicit validation.
- **Turnstile keys admin-managed** (migration 11 `20260902070000_add_turnstile_keys`): `SecuritySetting` + `turnstileSiteKey String?` (plaintext, public) + `turnstileSecretEnc String?` (AES-256-GCM, write-only). `PUT /api/admin/security` saves both; GET **never** returns the secret (`turnstileHasSecretKey` only); `''` clears both → env fallback. `TurnstileService.isConfiguredAsync()` **DB → env** (admin priority). `GET /api/public/auth-config` serves the **site key** to the widget (never the secret). UI `/manager/securite` panel « Clés Turnstile » (site key + secret « laisser vide = inchangé » / Effacer) + config badges.
- **Knowledge base double-audience** (migration 12 `20260902080000_init_knowledge`): `KnowledgeArticle` (audience ADMIN|CLIENT, type INFORMATIVE|TECHNICAL|HOWTO, status DRAFT|PUBLISHED|ARCHIVED, slug unique per audience, summary, body HTML, category, phase, tags, authorEmail denormalized). **Admin only** `GET/POST/PUT/DELETE /api/knowledge` (ADMIN, audit `knowledge.*`, auto-slug + `-2`/`-3` collisions). **Client public** `GET /api/client/knowledge` (only CLIENT + PUBLISHED, list WITHOUT body) + `.../categories` + `.../:idOrSlug` (client draft or admin article → NEVER exposed). Web `/manager/connaissance` (tabs Admin/Client, search, status filter, Publish/Unpublish, drawer editor) + `/aide` (client help center: hero + search + category chips, grouped cards, drawer reader with **defensive HTML sanitization** — script/iframe/on*/javascript: stripped before `dangerouslySetInnerHTML`).
- **Design & conversion overhaul (ADR-023 extended, target style app.arumdigital)** — landing `/` (hero « Votre hébergement, piloté depuis un seul endroit » + product showcase window + 6 feature cards + stats band + CTA) · `/offres` pricing catalog (cards by kind, Disponible badge, full-width Commander, 4-item reassurance) · `/auth` split layout (brand value aside → auth card, collapses < 880 px) · mobile nav drawer (animated hamburger → `mobile-nav` drawer + overlay + scroll lock + close-on-navigate, visible < 900 px) · glass topbar (`backdrop-filter` blur+saturate) · primary buttons gradient + glow + press spring (`--ease-spring`) · panel/stat-card hover lift (`--shadow-soft`/`--shadow-lift`) · ambient glows `--bg-glow-1/2` + fixed-attachment · `--radius-hero` 18 px · `.main` page-in animation. `brand.ts` `home` → `/`. Brand still ONLY in `config/brand.ts` + `--brand-*` tokens (no rebrand). Business logic of every converted page unchanged.
- **Everything NON-mandatory, admin-driven** (`SecuritySetting` singleton, 10th migration `init_security_support`, all 6 flags default **false**): `turnstileEnabled`, `oauthGoogleEnabled`, `oauthGithubEnabled`, `mfaRequiredForAdmins`, `selfRegistrationEnabled`, `deployEnabled`. `GET/PUT /api/admin/security` (ADMIN, audit `security.settings.update`); web `/manager/securite`.
- **Role ladder** `USER(0) < SUPPORT_L1(1) < SUPPORT_L2(2) < SUPPORT_L3(3) < ADMIN(99)`: `src/auth/roles.ts` `roleRank()` shared (RolesGuard + tickets + support); `RolesGuard` rank-based (`required.some(r => actorRank >= roleRank(r))`) — equivalent for all existing `@Roles(ADMIN)`; impersonation JWT pinned `role:USER` → reaches NO support/admin route. `manager.service.ts` byRole/total now 5 keys (same migration step as enum — compile break resolved).
- **Impersonation** "Se connecter en tant que client": `POST /api/users/:id/impersonate` (ADMIN) + `POST /api/auth/impersonate/return` + `POST /api/users/:id/mfa-reset` — **real routes are `/api/users/...` NOT `/api/admin/users/...` (documented ADR-027 divergence from plan; web `lib/api.ts` is source of truth)**. Token `{role:USER, imp:{by,kind}}` pinned at signature, **NO refresh cookie/row** (dies at TTL 60 min; `/auth/refresh` can't prolong), **READ-ONLY** (mutating verbs 403 unless `@AllowImpersonationMutation`), audit `impersonate.start/end`. Web red banner on `/client` + Revenir.
- **Support code 6-digit**: `SupportCode` model, `codeHash` = HMAC-SHA256(`SUPPORT_CODE_PEPPER ?? ENCRYPTION_KEY`, code) hex — never plaintext; ONE active per user; TTL default 60 min (clamp 5..1440); **lockout at 5 wrong attempts** (auto-revoke + audit `support.code.locked`); timing-safe hex compare, no short-circuit; ghost-code dummy compare. `POST/GET/DELETE /api/client/support-code` (USER) + `POST /api/support/access` (SUPPORT_L2+, Turnstile if enabled) → support impersonation READ-ONLY.
- **MFA TOTP + email OTP (self-service)**: `otplib` v13.5 functional API (Jest stub `test-utils/otplib.stub.ts` via moduleNameMapper — any 6-digit valid in tests; real email OTP timing-safe). `POST /api/auth/mfa/setup|confirm|disable` (secret AES-256-GCM `mfaSecretEnc`, never exposed); 2-step login when `mfaEnabled` (in-memory challenge store 300 s single-use, methods `['totp']+['email']` when mail can send, lockout 5, IP throttle); `mfaRequiredForAdmins` → ADMIN without MFA gets `{enroll:true,enrollToken}` (enroll token limited to setup/confirm); admin rescue `POST /api/users/:id/mfa-reset`. Web `/profil` (QR/secret + providers + password).
- **OAuth Google + GitHub (native fetch, ZERO dependency)**: injectable `OAuthProviderClient` (e2e-mockable via `GOOGLE_OAUTH`/`GITHUB_OAUTH` Symbol tokens). `GET /api/auth/oauth/:provider` → httpOnly state cookie `ihp_oauth_state` (10 min) → 302 provider; **`redirect_uri = ${publicBaseUrl}/api/auth/oauth/:provider/callback` NEVER :3001** (Next rewrite in dev). Callback: timing-safe state (mismatch → `error=oauth_state_mismatch`), **verified email**, scenario resolution: **login** (existing account → MFA → `issueTokens`; unknown email WITHOUT checkout intent → refused, never auto-create), **order-time REGISTER** (unknown email + valid `ihp_checkout` cookie → account + PENDING subscription), **LINK** (mode `link` with `sub` → attach `oauthProvider/oauthSubject` to connected account; conflict → `/profil?link=conflict`); `POST /api/auth/oauth/unlink`. `githubTokenEnc` stored (AES) for Phase 10bis. Audits `oauth.login/link/register`.
- **Order-time registration (email+pass / OAuth)** + **public catalog**: `GET /api/public/products` (NO guard; `GET /api/products` stays 401 — core.e2e intact); `POST /api/checkout/intent {productId}` (public) → signed `ihp_checkout` httpOnly cookie (10 min); `POST /api/auth/register` reopened but **REQUIRES valid intent (403 otherwise)** → account + PENDING subscription in `$transaction` + audit. Web `/offres` (public catalog → Commander → signup/login → order placed).
- **Minimal tickets**: `Ticket` + `TicketMessage` + enums `TicketStatus`/`TicketPriority`; escalate L2/L3 (`escalatedTo`); routes USER owner-or-support ≥ L1 (`roleRank`); `POST/GET /api/tickets`, `GET /api/tickets/:id` + messages, `POST .../escalate`, `PATCH .../status` (SUPPORT_L1+). Audit `ticket.*`. Web `/manager/support` + « Mes tickets » on `/client`.
- **Turnstile**: `TurnstileService` siteverify native fetch, skip if disabled/no key; enforced on login + support/access (400 on failure); web widget only if `NEXT_PUBLIC_TURNSTILE_SITE_KEY`. **Rate limiter**: `auth/rate-limiter.ts` in-memory sliding window per IP (429) on login/mfa-verify/mfa-email/support-access/register/checkout-intent.
- **Verification (2026-09-02, Phase 11)**: unit **223/223** (25 suites, +10: knowledge.service 8, security-settings 8 rewritten, turnstile isConfiguredAsync), e2e **132/132 18 suites** (+`knowledge` 3 tests, `security-settings` +1 turnstile-keys test — full non-regression of the 16 prior suites incl. `GET /api/products` 401), tsc api + web PASS, `web build` PASS **18 routes** (`.next` purged + dev stopped before build — Phase 2 lesson). Dev servers restarted: API :3001 health ok, web :3000 Ready. **Awaiting owner live validation → commit + push Phase 11 → Phase 10bis.**
- **Verification (2026-09-03, audit & corrections)**: unit **224/224** (+1 products 409 spec), e2e **132/132 18 suites** after the `maxWorkers: 1` isolation fix, tsc API+web PASS, `web build` PASS 18 routes, knowledge seed run on real DB (34 created → idempotent). **✓ Owner-validated 2026-09-03 → committed + pushed → Phase 10bis next (plan pending owner validation).**
Implementation is current. Summary of the stack:
- **Auth (Phase 1, ADR-015/016)**: JWT Bearer access (15m) + httpOnly refresh cookie (rotated, sha256-hashed, revocable), bcryptjs, ADMIN/USER RBAC + RolesGuard. Tables `User` + `RefreshToken` (migration `init_auth`). Web `/auth`.
- **Core model (Phase 2, ADR-017)**: tables `Product` + `Server` — PLATFORM-GLOBAL reference data, **no ownerId** (migration `init_core`). Product read = any authenticated; Product mutation & all Server routes = ADMIN only.
- **Admin console (Phase 3, ADR-018)**: `GET /api/users` + `PATCH /api/users/:id` (promote/demote, activate/deactivate) — ADMIN only, **anti-lockout guards**; `GET /api/manager/summary` — ADMIN only. Web `/manager` dashboard + catalog (status transitions, editable hostname) + `/manager/utilisateurs`.
- **Audit journal (Phase 4, ADR-019)**: table `AuditLog` (migration `init_audit`) — append-only, ADMIN read (`GET /api/audit`, paginated+filtered). `AuditModule` `@Global`. Web `/manager/journal`.
- **Invitations (Phase 5B, ADR-020)**: **`POST /api/auth/register` → 410 Gone** (inscription fermée). New accounts only via `POST /api/auth/accept-invite` (token + email + password + name). Table `Invitation` (migration `20260831084839_init_client_access`) — token sha256 (raw shown once), issuerId SetNull, TTL `INVITE_EXPIRES_IN_DAYS` default 7. ADMIN routes `GET/POST /api/invitations` + `POST /api/invitations/:id/revoke` (idempotent). Web `/manager/invitations`. **AuthModule ↔ InvitationsModule via `forwardRef` on both sides** (`acceptInvite` → `InvitationsService.consume` + existing `issueTokens`); `inviteExpiresInDays` optional in configuration.
- **Client workspace (Phase 5A, ADR-021)**: tables `Subscription` (userId Cascade, productId Restrict) + `Service` (subscriptionId Cascade, serverId nullable SetNull). Client routes (any authenticated): `GET/POST /api/client/subscriptions`, `PATCH …/:id/cancel`, `GET/POST /api/client/services`. ADMIN routes: `GET /api/admin/subscriptions` + `PATCH …/:id` (whitelist approve/reject/suspend/activate), `GET /api/admin/services` + `PATCH …/:id` (assign existing Server + REQUESTED→PROVISIONING→ACTIVE **stub**). **Ownership by possession in the service layer** (`where: {userId}` → another client's id is 404); **client listings use an explicit Prisma `select` WITHOUT `serverId`/`server`** — never expose infra. Provisioning is a status-transition stub (ADR-010/007 OUT; `Deployment` deferred). Web `/client` + `/manager/subscriptions`.
  - Prisma rule learned: `include` can't mix with scalar fields — use `.select` when you need scalars + relations (see `SERVICE_SELECT` in `subscriptions.service.ts`).
- **Mail (Phase 6, ADR-022)**: singleton `MailSetting` (migration `20260831120703_init_mail`) — `enabled`, `host`, `port`, `secure`, `user?`, `passwordEnc?`, `fromEmail`, `fromName?`. **Password AES-256-GCM at rest** (`CryptoService`, key = sha256(`ENCRYPTION_KEY`), payload base64 `iv||tag||data`; `ENCRYPTION_KEY` optional at boot, required at password save → clear 400; **never returned by the API**, only `hasPassword`). ADMIN routes `GET|PUT /api/admin/mail` (PATCH-semantics: `enabled=true` requires host+fromEmail → 400; `''` = clear on user/fromName; empty password = unchanged) + `POST /api/admin/mail/test` (uses saved config, independent of `enabled`, surfaces SMTP error as 400). Web `/manager/mail`.
  - **Invitations + email**: `InvitationsService.create` sends best-effort (never throws; `emailSent` in the return; audit `invite.email` `{email, emailSent, reason?}`); **the one-time token stays the manual fallback** in `/manager/invitations`. Link = `PUBLIC_BASE_URL + /auth?invite=<token>&email=<email>`.
  - Architecture: `MailService` is STATELESS (config passed in) to avoid the `MailSettingsService`↔`MailService` provider cycle; `MailTransportFactory` is the **test seam** — e2e overrides it (`overrideProvider(MailTransportFactory)`), so **no real SMTP is ever contacted in tests**. `MailModule` imports `AuthModule` via `forwardRef` (cycle Link **Mail→Auth→Invitations→Mail** at the module-file level — same pattern as Auth↔Invitations). `CryptoModule` is non-global. `publicBaseUrl` in config.
  - Env: `ENCRYPTION_KEY` + `PUBLIC_BASE_URL` in `.env.example` AND in local gitignored `apps/api/.env` (dev-only values) — e2e needs the key when a test saves an SMTP password.
- **Admin bootstrap**: `db:seed` (idempotent, from gitignored `.env` ADMIN_EMAIL/ADMIN_PASSWORD).
- `@nestjs/jwt` pinned `^11.0.2` (CJS) to keep jest/tsc happy. nodemailer 9.1.0 (CJS, jest-safe).
- Tests: unit **259/259** (27 suites, +35 Phase 10bis: github.service, deployments.service 15, panel-transport +6), e2e **145/145 19 suites** (+`deployments` 13 tests, fake transport + fake GithubService, zero network). **`maxWorkers: 1` in `test/jest-e2e.json` (added 2026-09-03)** — REQUIRED for the e2e suites: shared Postgres + singleton `SecuritySetting` row + parallel workers clobber each other (seen: oauth/security-settings failing together, passing alone). Existing e2e suites create test users **directly via Prisma** (self-registration is closed except order-time intent flow), except auth e2e which exercises invite→accept→login. `testTimeout: 30000` in `test/jest-e2e.json`. **otplib ESM**: Jest `moduleNameMapper ^otplib → src/test-utils/otplib.stub.ts` (verifySync always valid — TOTP accepts ANY 6-digit in tests; real email OTP stays timing-safe). OAuth tests mock the provider client (`overrideProvider`), so zero network.
- Dev servers: api :3001 running (nest watch, left up); web :3000 on demand — start it WITHOUT running `web build` in parallel (`.next` corruption). **If `next dev` hangs at « Starting... »: kill ALL lingering `next dev`/node on :3000, purge `apps/web/.next` completely, then start ONE clean `next dev`** (Ready in ~9 s).
- **Phase 6 owner-validated live (2026-08-31)**: real Brevo SMTP (`smtp-relay.brevo.com:587`, user `9bda29001@smtp-brevo.com`), domain **codediali.com** bought + linked/validated in Brevo, fromEmail `contact@codediali.com` — test emails confirmed **`delivered`** in Brevo Logs→SMTP. Live lessons: (1) Brevo `525 5.7.1 Unauthorized IP address` → authorize the public IP in the Brevo account (the owner's IP is **dynamic ADSL** — will need re-authorization after IP changes); (2) **unvalidated sender** → Brevo accepts the SMTP session (API shows `ok:true`) but rejects the message ASYNCHRONOUSLY — check Brevo → Logs → SMTP (`error` event "sender not valid"); always use a validated sender/domain (e.g. the codediali.com one).
- **Design system (Phase 7, ADR-023)**: complete visual rewrite of the front — **NO API/DB change** (no migration, no API test). Reference HTML dashboard's dark/light palette copied **exactly** into CSS variables (`apps/web/src/app/globals.css`, brand green `#00b377`); brand lives ONLY in `config/brand.ts` + `--brand-primary*` tokens — **rebrand Code Diali = edit those two places only** (deferred by owner until the project is finished). Full spec + AI style rules: `docs/design/DESIGN_SYSTEM.md`.
  - **Theme**: `data-theme` on `<html>` (dark default), persisted `localStorage 'ihp-theme'`, **anti-FOUC inline script** in `layout.tsx` (before paint), `<html lang="fr">`, `ThemeToggle` in the topbar.
  - **Components** (`apps/web/src/components/`): `icons.tsx` (~20 inline SVGs, zero dep), `ui.tsx` (Button/Badge/Alert/Panel/StatCard/Field/Input/Select/PageLoading/EmptyState/PageIntro/Denied/statusTone), `app-shell.tsx` (topbar + sidebar + nav active + logout + `bare` mode for centered auth screens), `theme-toggle.tsx`. `config/nav.ts` (ADMIN_NAV 6 entries + client). `lib/session.ts` (`useAdminSession`) centralizes the admin bootstrap that was duplicated across 6 pages.
  - **9 pages refactored** (`/`, `/auth`, `/manager*` ×6, `/client`) — **business logic unchanged** (same calls/state/handlers), classes only (no inline business styles). Plain CSS only (no Tailwind/framework), per the reference.
  - Windows/dev note: start `next dev` WITHOUT running `web build` in parallel (`.next` corruption, Phase 2 lesson).
- **Polish UI (Phase 7bis, ADR-023 follow-up — 2026-08-31, owner feedback on Phase 7)** — pure front, **NO API/DB change**. Owner: « couleurs et style à ne pas changer » + 4 asks:
  - **Selects** (`.select`/`.select-sm` in `globals.css`): `appearance:none` + chevron SVG data-URI (color = `--text-secondary` per theme), `padding-right:36px`, heights = `.input`/`.btn` → aligned with adjacent buttons in tables; `<option>` themed. No page JSX changed.
  - **Light border contrast**: ONLY the 2 light tokens changed — `--border #e7eaf0 → #d5dce8`, `--border-soft #eef0f4 → #e1e6ef`. Dark + all other colors untouched.
  - **Alert spacing**: `.alert` `margin-bottom:12px`, reset to 0 inside `.stack`/`.panel-body` (gap handles it).
  - **Toasts**: new `apps/web/src/components/toast.tsx` — `ToastProvider`/`useToast()` (`ok/error/info/warn`), mounted in `layout.tsx` (covers all pages incl. `/auth` bare). Pop-up top-right: icon + message + **OK button** (instant dismiss), **auto-dismiss 5 s** (timers cleaned on unmount), `role=status/alert` + `aria-live`, enter animation ~0.18 s. 8 pages converted from `message`/`error`/`testResult` states → toasts (handlers unchanged). **Kept inline by design** (persistent context, not action feedback): home `/` diagnostic alert + the invitations `created` panel (token + link to copy).
- **Rebrand (deferred by owner)**: brand "iCode Host Pro" → **Code Diali** (`codediali.com`) once the project is finished — recorded in TASKS/PROJECT_STATUS, not yet executed.
- **Server/Product admin management + infra details (Phase 7ter, ADR-024)** — 2026-09-01. Owner (AskUserQuestion) chose dedicated **Produits** page too + 7 new server fields. Purpose: the `Server` record grows toward real server connections (future ADR-010 adapters / ADR-007 jobs); the admin UI is built for it NOW.
  - **Model**: `Server` extended (migration `20260901021234_init_server_details`, now **6 migrations**) — all OPTIONAL: `ipAddress?`, `port?` (1–65535), `provider?`, `region?`, `quotaMaxAccounts?`, `strictTls @default(true)`, `panelProvider` enum `ServerPanelProvider {NONE HESTIA COOLIFY}` (comment: cPanel/DirectAdmin next). Non-breaking: creating a server still requires only name+hostname; initial status UNKNOWN.
  - **API**: `CreateServerDto` +7 optional validated fields; `ServersService.create` explicit whitelist; tests updated (+full-details unit; +e2e ADMIN POST all fields → 201 + PATCH panelProvider/port → 200). Unit **91/91**, e2e **62/62 (8 suites)**.
  - **Web**: sidebar items **Serveurs** + **Produits** (`config/nav.ts`); `wrap-md` 900 → **1320px** + `.table-wide` (the old 2-col dashboard overflow is structurally gone); pages `/manager/serveurs` (wide CRUD table, full **inline editing**, creation grid-form — "statuses will be driven by the real server connection (coming)") and `/manager/produits` (CRUD); **dashboard `/manager` now READ-ONLY** (stats + last-6 synopsis + "Gérer →" links + quick-links panel, NO forms/handlers). `lib/api.ts`: `ServerAdmin`/`ServerPatch`/`ProductAdmin`/`PanelProvider` + CRUD helpers.
  - **Future-proofing**: keep every new Server field optional (forced-by-connection fields are NOT mandatory at creation); `panelProvider` is the seam for ADR-010 (Coolify/Hestia adapters then cPanel/DirectAdmin); provisioning stays a status stub (ADR-021). Web build note: `web build` was run with dev web STOPPED + `.next` purged (Phase 2 lesson), then `next dev` restarted.
  - **Environment happened to be DOWN (2026-09-01)**: Docker Desktop was off → postgres down + dev servers stopped ("le web n'est pas accessible"). Fix: docker up (postgres healthy) + API :3001 + web :3000. This was environment state, NOT a code regression — if the web is ever "not accessible", first check `docker stats`/ports 3000/3001/5432 before touching code.
  - Awaiting: ~~owner live validation + commit~~ → **OWNER-VALIDATED 2026-09-01 ✓, committed `1d7131e`, pushed to `origin/main`** (with 31af3e2 design + 76fb0db polish). Next: proposal for Phase 8.
- **Real server connectivity probe (Phase 8, ADR-025)** — 2026-09-01. Owner (AskUserQuestion) chose « **Connexion réelle des serveurs** »: narrow ADR-010 slice — a connectivity probe (TCP + HTTP) the admin triggers from `/manager/serveurs`, which detects the real network state and **proposes** a status switch. Real provider adapters / credentials / auto-provisioning stay OUT of scope.
  - **Model**: `Server` +3 nullable probe-result fields (migration `20260901082020_init_server_check`, now **7 migrations**) — `lastCheckedAt DateTime?`, `lastProbeOk Boolean?` (null = never probed), `lastProbeDetail String?`. Written ONLY by the probe (create/patch whitelist unchanged).
  - **Seam**: `ProbeTransportFactory` (`src/servers/probe-transport.factory.ts`) mirrors `MailTransportFactory` — `ServersService` depends on the factory, e2e overrides it (`overrideProvider`) → **no real network in tests**; unit tests the real transport against loopback. Runtime `NodeProbeTransport`: TCP (`net.connect`, ports like SSH, default 22) + HTTP/HTTPS (80/443/8443, `rejectUnauthorized = strictTls`) ; `probeMode` forces HTTP (ephemeral-port unit test) ; any HTTP response = reachable (even 5xx) ; default timeout 5 000 ms ; readable detail ("TCP 22 : accessible (18 ms)", "HTTP 200 en 45 ms", "Connexion refusée", "Délai dépassé (5 000 ms)", "Erreur TLS : …", "Hôte introuvable").
    - **Nest DI lesson (Phase 8)**: a class @Injectable must NOT inject a primitive (`Number`) via its constructor — Nest resolves it as an unfindable token and breaks AppModule init for ALL suites (seen when the whole e2e run except `server-check` failed with "Cannot read properties of undefined"). The timeout lives in `create(timeoutMs)`, overridable per test.
  - **API**: `POST /api/servers/:id/check` (ADMIN): target `hostname` + `port` (default 22) → probe → **persists** the 3 fields → audit `server.check` `{host, port, ok, detail, latencyMs, httpStatus, statusLeft}` → `{ server, probe }`. **The status is NEVER forced** — the probe writes `lastProbe*` and proposes; the admin validates.
  - **Web**: `/manager/serveurs` new **Connexion** column — persistent badge (OK/Échec/— = never tested) + `lastProbeDetail`, a **« Tester »** button (spin while probing), and a quick `→ ACTIVE` (probe OK) / `→ PROBLEM` (probe failed) toggle = a plain admin PATCH (+ `server.update` audit). Journal label `server.check` = « Test de connexion serveur ». `lib/api.ts`: `ServerAdmin` +3 probe fields, `ServerProbe`, `ServerCheckResult`, `checkServer(t, id)`.
  - **UX rework (Phase 8 bis, owner feedback — « style très moderne pour l'affichage des tableaux ou données modifiables »)**: `/manager/serveurs` table 11 cols + inline edit → **card grid** (toolbar search name/hostname/ip/provider/region + status filter chips `srv-chip` with counters, memory-only; cards `srv-card` with status-tinted icon + name + status badge + mono hostname, 2-col fields IP/Port mono + provider/region/quota + TLS/Panneau badges, integrated **Connexion** block OK/Échec/— (dot badge) + detail + « Dernier test » + Tester + `→ ACTIVE`/`→ PROBLEM`, hover-revealed **Modifier**/Supprimer) + **side drawer** (`drawer-*`: overlay blur + slide-in 0.22s, Esc/backdrop close when not busy, status select on edit only, submit `Créer le serveur`/`Enregistrer`). New icons `IconSearch`/`IconPencil`/`IconTrash`. Handlers/validations/toasts + probe logic UNCHANGED. Front-only — no API/DB/migration/test change. `.next` purged + dev web stopped before build (Phase 2 lesson).
  - **Tests**: unit 98/98 (+7: check success/port-default, failure/explicit-port, 404; transport-on-loopback HTTP 200/TCP ok/refused/host-not-found) ; e2e **67/67, 9 suites** (+server-check 5 tests, seam override, zero network). Typecheck + web build PASS (14 routes, `/manager/serveurs` 6.39 kB). Live smoke :3001 — `localhost:5432` → "TCP 5432 : accessible (8 ms)" OK persisted+re-read ; `127.0.0.1:5999` → "Connexion refusée" persisted ; audit `server.check` populated.
  - Awaiting: **owner live validation → commit + push**. A future automatic status policy (periodic probe → status) is mentioned but OUT of scope (ADR-007/010 stay PROPOSED).

Reported decisions: ADR-001..005 + 011..014 APPROVED (Phase 0); ADR-015+016 (Phase 1); **ADR-017 (Phase 2)**; **ADR-018 (Phase 3)**; **ADR-019 (Phase 4)**; **ADR-020 + ADR-021 (Phase 5)**; **ADR-022 (mail settings + invitation emails, Phase 6 — validates a NARROW ADR-008 slice: app-level encryption at rest)** **APPROVED (2026-08-31)**; **ADR-023 (interface design system, Phase 7 — palette copied from the owner's reference, brand-agnostic, everything modifiable)** **APPROVED (2026-08-31)**; **ADR-024 (Server infra details + wide admin CRUD pages, Phase 7ter — optional fields for future real connectors, panelProvider enum, read-only dashboard, 1320px layout, GO via AskUserQuestion)** **APPROVED (2026-09-01)**; **ADR-025 (real server connectivity probe — TCP/HTTP, test seam, check endpoint + audit, status never forced — first real step toward ADR-010, GO via AskUserQuestion) APPROVED (2026-09-01)**; **ADR-026 (server metrics + IP/port auto-detection + direct access, Phase 9bis — owner request) IMPLEMENTED 2026-09-02 (recorded, owner validation pending)**; **ADR-027 (security, accounts & support — Phase 10: NON-mandatory admin flags, role ladder, read-only impersonation, HMAC support code, self-service MFA, order-time OAuth/email+password registration, account linking, minimal tickets, Turnstile, rate limiter — plan APPROVED by owner)**; **ADR-028 (knowledge base + admin-managed Turnstile keys — Phase 11, owner request: informative/TECHNICAL/how-to articles, admin-only + client audience, PUBLISHED gating; the design/UX conversion & mobile overhaul is an extension of ADR-023) IMPLEMENTED 2026-09-02 (recorded)**; **ADR-029 (traffic, quotas & suspension — 4-layer architecture measure/quota/limit/suspend; analysis in `docs/traffic-quotas.md`; `quotaMaxAccounts` currently NOT enforced, suspension manual via `isActive`) PROPOSED 2026-09-03 — intentionally NOT in the APPROVED list, awaiting owner validation, nothing implemented**; **ADR-030 (GitHub → Coolify deploy — Phase 10bis, plan « go » 2026-09-03) IMPLEMENTED (recorded, awaiting owner live validation)** — auto-detected GitHub repos, `PanelTransport` createGitApp/deployApp/deploymentStatus (create route confirmed live), `Deployment` model (coolifyUuid never exposed), ROOT/write token requirement (observed live); ADR-006 full, 007, 008 full, 009 stay PROPOSED.

## Common commands (dev)
- Install:   `corepack pnpm install`
- Postgres:  `docker compose up -d postgres` / `docker compose ps`
- Prisma:    `corepack pnpm --filter @icode-host-pro/api generate` / `... run migrate --name <name>` (use `--name`, never the literal `--` origin it into an interactive prompt)
- Seed admin:`corepack pnpm --filter @icode-host-pro/api run db:seed` (idempotent; requires ADMIN_EMAIL/ADMIN_PASSWORD in apps/api/.env)
- Seed knowledge:`corepack pnpm --filter @icode-host-pro/api run db:seed:knowledge` (idempotent create-if-missing by `[audience, slug]`; 34 articles admin+client, never overwrites admin edits; author = oldest ADMIN)
- Tests:     `corepack pnpm --filter @icode-host-pro/api test` / `... test:e2e`
- Servers:   `corepack pnpm --filter @icode-host-pro/api start` (quai :3001) ; `corepack pnpm --filter @icode-host-pro/web dev` (quai :3000)
- Build:     `corepack pnpm build`
Note: pnpm runs via `corepack pnpm ...` on this machine (corepack `enable` is blocked by a protected Program Files dir; the subcommand form needs no admin).

## After every phase
Update TASKS.md, PROJECT_STATUS.md, CHANGELOG.md, docs/sql-commandes.txt for database work, DECISIONS.md only for genuine decision changes, and HANDOVER.md when continuation instructions change.

## Never
Never fabricate approval, erase history, commit secrets, assume existing code is correct or perform major architecture changes without recording them.
