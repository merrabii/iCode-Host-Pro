# Code Diali API — development config socle (Phase 0/1, ADR-011).
# ADR-008 (secrets, encryption, persisted config) remains PROPOSED — not covered here.

NODE_ENV=development

# TCP port the NestJS HTTP server listens on
PORT=3001

# PostgreSQL connection string (matches docker-compose.yml, ADR-012)
DATABASE_URL=postgresql://icode:icode@localhost:5432/icode_host_pro?schema=public

# Global REST prefix, also used for Swagger docs (see ADR-005)
API_PREFIX=api

# JWT access token signing secret. DEV-ONLY value below; MUST be a strong random
# secret in any real deployment (Phase 1 / ADR-015). Do not commit a real secret.
JWT_SECRET=dev-only-change-me-use-a-random-secret-in-production-2f8b0c2f

# Access token lifetime (jsonwebtoken duration syntax)
JWT_EXPIRES_IN=15m

# Refresh token lifetime in days (persisted, rotated, revocable)
REFRESH_EXPIRES_IN_DAYS=30

# httpOnly cookie name carrying the refresh token
COOKIE_NAME=ihp_refresh

# Phase 5 — ADMIN-issued invitation lifetime in days (ADR-020). Optional.
INVITE_EXPIRES_IN_DAYS=7

# Phase 6 — master key for encryption at rest (ADR-022): SMTP password stored
# AES-256-GCM-encrypted. OPTIONAL and used only when an admin saves an SMTP
# password; any length accepted (sha256-derived). Random example below — use a
# strong random value and keep it stable across restarts (don't rotate blindly).
ENCRYPTION_KEY=dev-only-change-me-to-a-random-secret-4f9a7c21

# Phase 6 — public base URL used to build absolute invitation links in emails
# (ADR-022). Production: the public host of the web app.
PUBLIC_BASE_URL=http://localhost:3000

# Allowed web origin for CORS (absolute/non-proxy usage)
WEB_ORIGIN=http://localhost:3000

# Trust proxy (Express) — vide/false par défaut : X-Forwarded-For est IGNORE et
# req.ip = adresse socket (API jointe directement). Ne renseigner QUE si un
# reverse proxy précède l'API : IP/CIDR des proxies approuvés, séparés par des
# virgules (ex. 172.18.0.0/16), ou un preset : loopback | linklocal | uniquelocal.
# La valeur littérale "true" est REFUSÉE (XFF forgeable). Avec Cloudflare devant :
# <IP du proxy frontal>, puis les plages IP publiées de Cloudflare.
TRUST_PROXY=

# Phase 2 — platform ADMIN bootstrap (ADR-017). Placeholders ONLY — put real values
# in apps/api/.env (gitignored); never commit a real admin credential.
# Seed: corepack pnpm --filter @icode-host-pro/api run db:seed
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-strong-password

# ── Phase 10 (ADR-027) — optional security keys (all default to DISABLED) ─────
# Every security feature is NON-mandatory and admin-toggleable (SecuritySetting).
# Leaving these blanks means the corresponding feature cannot run even if the
# admin enables it in /manager/securite — the service degrades safely.

# Cloudflare Turnstile (anti-bot on login / support access). Site key = public
# (goes in NEXT_PUBLIC_TURNSTILE_SITE_KEY on web); secret key = here.
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Google OAuth (only used when oauthGoogleEnabled in SecuritySetting)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth (only used when oauthGithubEnabled in SecuritySetting).
# Scope `user:email` for verified email; `repo` is added for Phase 10bis
# (private-repo auto-detection / Coolify deploy) when requested by the client.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# HMAC pepper for hashing the 6-digit support codes. Blank → falls back to
# ENCRYPTION_KEY (still HMAC, never stored in clear).
SUPPORT_CODE_PEPPER=
# Support code TTL in minutes (clamped 5..1440). Default 60.
SUPPORT_CODE_TTL_MINUTES=60

# Email-OTP / MFA challenge TTL in seconds. Default 300.
MFA_OTP_TTL_SECONDS=300

# Impersonation access-token TTL (JWT duration syntax; default 60m, capped in
# the service at 24h). Impersonation sessions carry no refresh token/cookie and
# cannot be prolonged.
IMPERSONATION_EXPIRES_IN=60m

# OAuth `state` cookie TTL in seconds. Default 600.
OAUTH_STATE_TTL_SECONDS=600

# ── Phase 17B — réconciliateur asynchrone des déploiements ─────────────────────
# Moteur conservant DEPLOYING les apps qui n'ont pas été prouvées en ligne après
# le proof-gate borné, avec backoff borné et confirmation terminale. Aucune
# boucle/timer ici (17B.4B) : un worker viendra en 17B.4C. Chaque valeur invalide
# bascule sur son défaut (jamais de crash) ; les valeurs ci-dessous = défauts.
# Désactivé par défaut : il doit être activé volontairement (GO live 17B.4C+).
RECONCILE_ENABLED=false
# Intervalle de scan (10 000..900 000 ms). Défaut 30 000.
RECONCILE_SCAN_INTERVAL_MS=30000
# Taille maximale du batch de candidats par cycle (1..100). Défaut 10.
RECONCILE_BATCH_SIZE=10
# Durée du lease/claim par candidat (30 000..1 800 000 ms) — toujours supérieure
# à la durée maximale normale d'une observation (panel + preuve HTTP). Défaut 120 000.
RECONCILE_LEASE_MS=120000
# Seuil d'ALERTE du nombre de tentatives (mode lent/monitoring, jamais un arrêt,
# jamais un FAILED). Alerte auditée exactement une fois au franchissement. 1..100. Défaut 12.
RECONCILE_ATTEMPT_ALERT_THRESHOLD=12
# Délai de backoff initial (n=1), 10 000..1 800 000 ms. Défaut 30 000.
RECONCILE_BACKOFF_INITIAL_MS=30000
# Délai de backoff maximal (palier lent, pas de famine), 60 000..86 400 000 ms,
# toujours >= backoffInitialMs. Défaut 3 600 000.
RECONCILE_MAX_BACKOFF_MS=3600000