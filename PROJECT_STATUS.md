# PROJECT_STATUS — Code Diali

## Overall status
**PHASE 16 (ADR-035/036) IMPLÉMENTÉE ET POUSSÉE — ÉTAT ACTUEL : PHASE 16 (store order-driven + plan gratuit + build config fichier), commits `539cacd` (16b) → `4705366` (16 store) → `11d3592` (e2e) → `2ed6a76` (docs arch). Le 2026-09-13, la validation réelle de bout en bout a RÉUSSI : déploiement d'un dépôt GitHub public (mode URL, SANS compte lié) → app SPA live `HTTP 200`. 34 migrations in sync.** Récapitulatif de la séquence livrée après la 10bis :
- **Phase 3 Cloudflare DNS** (`5395840` 2026-09-05, ADR-033) : contrôle DNS Cloudflare admin (`/manager/dns`, clé AES-256-GCM jamais renvoyée, zones racines importables, enregistrements live proxied, domaine racine), **sous-domaine client alloué auto au déploiement** (CNAME → hostname Coolify, `fqdn` unique exposé) ; **quota disque clean-up** (machinerie `--storage-opt` supprimée, `diskGb` → `storageLimit`, enregistré+affiché mais quota **INACTIF** — branché après mise en prod).
- **Phase 12** (`1d2527d` 2026-09-05, ADR-031) : catalogue **Produit → Catégorie + Pack** avec **limites réellement appliquées à Coolify** (`applyPack` → `PATCH /applications` RAM/CPU AVANT `deployApp` — qui passe à **`POST /applications/{uuid}/deploy`**, vérifié live ; serveur Coolify cibles configurables `coolifyProjectUuid`/`coolifyServerUuid` — `'0'` absent sur 4.1.2). Pages `/manager/packs` + `/manager/categories`. 297 unit + 149 e2e verts, validation réelle Coolify (app « Test limits pack » `limits_cpus=1`/`limits_memory=1g`).
- **Phase 13** (`0bfd856`→`52cae27` 2026-09-06/07, ADR-032 « ADR Module », 7 parties) : **modules de déploiement A/B** (`DeploymentModule` + enum `SHARED_PROJECT | PER_CLIENT_PROJECT`, module A = projet partagé / module B = **projet client dédié** `ClientProject` créé paresseusement), **quota d'apps** (`HostingPack.maxApps`, N/M affiché), **client déploie sans Service** (`Deployment.serviceId` optionnel), **monitoring** (`/manager/monitoring` agrège par projet Coolify) + admin users Module B, **dashboard `/client` moderne** (cartes apps quota, **suppression d'app 2 étapes** — purge app + sous-domaine Cloudflare, **upgrade pack**), fenêtre de réutilisation **10 s** sur refresh, +5 articles admin connaissance, `start.sh`.
- **Phase 14** (`2cf7b1d` + `8e5abcf` 2026-09-08, ADR-034) : **plateforme multi-brand / white-label** — tout détail de marque éditable depuis `/manager/apparence` (**`BrandConfig` singleton**, `GET /api/brand` public + ADMIN PATCH/reset/upload/remove), **logo fichier réel** (PNG/JPEG/WebP ≤ 2 Mo, SVG refusé, **`logoUrl` stocké sous `/api/branding/`** — fix ORB), **image seule / image+texte** (`logoShowText`), **nom de marque facultatif**, couleur appliquée **sans flash** (style `:root` injecté, verts dérivés `color-mix(var(--brand-primary))`), `BrandProvider`/`BrandLogo`, `config/brand.ts` = défauts. **Validé par le propriétaire** ; rebrand Code Diali **différé à la fin du projet**.
Règles conservées : **ne jamais pousser sans ok explicite** · rebrand Code Diali différé · SVG/interdit + ≤ 2 Mo · ADR-029 **PROPOSED** (quotas/suspension non implémentés).

## Current phase
**Phase 16 — Store order-driven + plan gratuit + build config fichier (2026-09-11/12, ADR-035 + ADR-036)** : abonnements **créés ACTIVE par la commande** (le paiement vaut approbation), table **`Service` supprimée**, **plan gratuit sans checkout** (`free-signup` / OAuth `mode=free`), **`codediali.toml` / `netlify.toml`** pré-remplis au build + détection SPA Vite (`isStatic` + `publish_directory:/dist`). **Validé REAL 2026-09-13 par un test auto-exécuté** : un compte gratuit colle l'URL d'un dépôt public (module B = projet client dédié créé sur Coolify), build nixpacks SPA → `ACTIVE` → `https://code-diali-guide-de-demarrage.arumdigital.com` **HTTP 200**. Le `403 "You are not allowed to access the API"` (vu sur `createProject` ET `createGitApp`) était la **liste blanche d'IP Coolify** (jeton ROOT déjà valide) — une fois l'IP autorisée, le déploiement passe. **Une fois le projet terminé** : rebrand → Code Diali (codediali.com), sans toucher au vert `#00b377`.

## State (real, as of this update)
- Monorepo: pnpm workspaces + Turborepo (ADR-001). `apps/web` (Next.js 15), `apps/api` (NestJS 11), `packages/` reserved.
- API: `GET /api/health` (app + real DB connectivity via `SELECT 1`), validated startup config socle (ADR-011), global `/api` prefix, dev CORS, OpenAPI/Swagger at `/api/docs` (ADR-005).
- **Auth (Phase 1, ADR-015)**: stateless JWT Bearer access (short-lived `15m`) + refresh httpOnly cookie (rotated, revocable, sha256-hashed). bcryptjs. RBAC `ADMIN | USER` + guards. **Phase 5 (ADR-020) : `POST /api/auth/register` → 410 Gone (inscription fermée)**; `POST /api/auth/accept-invite` crée le compte USER.
- DB: migrations `init_auth`, `init_core`, `init_audit`, `init_client_access`, **`20260831120703_init_mail`** (**`MailSetting`**), **`20260901021234_init_server_details`** (**Server : `ipAddress?` `port?` `provider?` `region?` `quotaMaxAccounts?` `strictTls` `panelProvider` enum `ServerPanelProvider {NONE HESTIA COOLIFY}`**) et **`20260901131323_init_server_panel`** (**Phase 9/ADR-010 : `apiBaseUrl?` `apiTokenEnc?` `apiUser?` `panelVerifiedAt?` `panelOk?` `panelDetail?`**). `prisma migrate status` → up to date (**8 migrations**). Prisma client 6.19.3.
- **Serveurs / Produits (Phase 7ter, ADR-024)** — registre d'infrastructure évolutif :
  - `Server` étendu (tous champs optionnels) pour accueillir les futurs connecteurs réels (ADR-010) : IP, port, fournisseur, région, quota max comptes, **TLS strict** (`strictTls` défaut true), **module de panneau** `panelProvider` (HESTIA/COOLIFY d'abord — cPanel/DirectAdmin ensuite). Statut initial UNKNOWN ; PROVISIONING/ACTIVE/PROBLEM seront pilotés par la connexion réelle (aujourd'hui saisis manuellement).
  - **Pages admin larges** : `/manager/serveurs` (CRUD + édition inline complète) et `/manager/produits` (CRUD) — ADMIN-only, sidebar items ajoutés. **Dashboard `/manager` lecture seule** (stats + synthèse + liens « Gérer → », zéro édition). Conteneur `wrap-md` 900 → **1320px** + `.table-wide` (le débordement serveur↔produits de l'ancien dashboard 2 colonnes est éliminé structurellement).
- **Invitations (Phase 5B, ADR-020)**: jeton one-shot, TTL 7 j, routes ADMIN, web `/manager/invitations`. **Phase 6 : envoi email automatique best-effort** (`emailSent` dans le retour ; token manuel conservé en fallback si config absente/échec); audit `invite.email` `{email, emailSent, reason?}`.
- **Mail (Phase 6, ADR-022)**: singleton `MailSetting` (enabled, host, port, secure, user, `passwordEnc`, fromEmail, fromName). **Password AES-256-GCM at rest** (`CryptoService`, clé = sha256(`ENCRYPTION_KEY`), payload base64 iv||tag||data) — **jamais renvoyé par l'API**, seulement `hasPassword`. `ENCRYPTION_KEY` optionnelle au boot, requise à l'enregistrement d'un password (400 clair sinon).
- Routes ADMIN `GET/PUT /api/admin/mail` (PATCH-semantics ; `enabled=true` requiert host+fromEmail → 400 ; `''` = effacé sur user/fromName) + `POST /api/admin/mail/test` (utilise la config enregistrée, indépendant de `enabled` ; remonte l'erreur SMTP dans un 400). `MailService` sans état (évite le cycle de providers), `MailTransportFactory` = couture de test overridée en e2e (aucun SMTP réel). Audit `mail.settings.update` (masqué) / `mail.test` (ok/error).
- Web: **`/manager/mail`** (formulaire SMTP : Activer, host, port 465/587/25, secure, user, password « inchangé si vide », fromEmail, fromName ; badge Configuré/Non configuré + warning hasPassword ; section test SMTP). `/manager/invitations` : ✅ email envoyé à X sinon ⚠️ bannière + lien manuel toujours copiable. Nav `/manager` + lien « Configuration mail ».
- **Module wiring**: `MailModule` (AuthModule via `forwardRef` — cycle d'import Mail→Auth→Invitations→Mail), `CryptoModule` (non-global). Invitations → MailSettingsService (best-effort, never throw). `publicBaseUrl` (`PUBLIC_BASE_URL`) pour les liens absolus des emails, défaut localhost:3000.
- **Design system (Phase 7, ADR-023)** — réécriture visuelle complète du front (aucun changement API/DB) :
  - **Tokens CSS** (`apps/web/src/app/globals.css`, ~1 100 lignes) : palette dark/light de la référence copiée **à l'identique** (bg `#070c1f` / light `#f8fafc`, sidebar, header, card, borders, text, active…), teintes badges/icônes (green/blue/violet/amber/cyan/pink/gray + rouge ajouté pour les erreurs), tokens marque `--brand-primary #00b377` / `--brand-primary-dark #009966` / gradient / glow, polices, sidebar 280px / topbar 64px, rayons, ombres, breakpoints responsive (<1100 / <900 / <600). Classes composants : topbar, sidebar, nav active, hero, stats, panels, table, badges, boutons, inputs, alertes, empty/spinner, auth-card, utils.
  - **Brand-agnostic** : la marque vit uniquement dans `apps/web/src/config/brand.ts` + tokens `--brand-primary*`. Rebrand différé Code Diali = modifier ces deux endroits seulement.
  - **Thème** : `data-theme` sur `<html>` (dark par défaut), persisté `localStorage 'ihp-theme'`, **script anti-FOUC** inline dans `layout.tsx`, bascule `ThemeToggle` dans la topbar. `layout.tsx` `<html lang="fr">`.
  - **Composants partagés** : `components/icons.tsx` (~20 icônes SVG inline), `components/ui.tsx` (Button/Badge/Alert/Panel/StatCard/Field/Input/Select/PageLoading/EmptyState/PageIntro/Denied/statusTone), `components/app-shell.tsx` (topbar + sidebar + nav + logout + mode `bare`), `components/theme-toggle.tsx`, `lib/session.ts` (`useAdminSession` — bootstrap admin dédupliqué des 6 pages), `config/nav.ts` (ADMIN_NAV 6 entrées + Espace client).
  - **9 pages refactorées** (`/`, `/auth`, `/manager`, `/manager/utilisateurs`, `/manager/journal`, `/manager/invitations`, `/manager/mail`, `/manager/subscriptions`, `/client`) — **logique métier inchangée** (mêmes appels/états/handlers), zéro style inline métier. Documentation : `docs/design/DESIGN_SYSTEM.md`.
- Docker Postgres 16 (ADR-012) up and healthy.

## State — Phase 10 (ADR-027, sécurité/comptes/support — migration 10 `20260902063000_init_security_support`)
- **SecuritySetting singleton** (pattern MailSetting) — 6 booleans défaut OFF : `turnstileEnabled`, `oauthGoogleEnabled`, `oauthGithubEnabled`, `mfaRequiredForAdmins`, `selfRegistrationEnabled`, `deployEnabled`. `GET/PUT /api/admin/security` (ADMIN, audit `security.settings.update`). Page `/manager/securite`.
- **Hiérarchie rôles** : enum `Role` + `SUPPORT_L1/L2/L3` (migration 10 : `ALTER TYPE "Role" ADD VALUE`). `src/auth/roles.ts` : `ROLE_RANK = {USER:0, SUPPORT_L1:1, SUPPORT_L2:2, SUPPORT_L3:3, ADMIN:99}` + `roleRank()` ; `RolesGuard` par rang. `manager.service.ts` `byRole`/`total` 5 clés (même pas que l'enum — casse compile résolue).
- **User étendu** : `mfaSecretEnc?` (AES-256-GCM), `mfaEnabled`, `oauthProvider?`, `oauthSubject?`, `githubTokenEnc?` (store Phase 10bis), `@@unique([oauthProvider, oauthSubject])`. `toPublic()` retire `passwordHash` + `mfaSecretEnc` + `githubTokenEnc`, expose `mfaEnabled`/`oauthProvider` — **jamais les secrets**.
- **Impersonation** : `POST /api/users/:id/impersonate` (ADMIN) + `POST /api/auth/impersonate/return` + `POST /api/users/:id/mfa-reset` (secours) — **routes réelles `/api/users/...` (pas `/api/admin/users/...`, divergence documentée ADR-027)**. Jeton `{ role: USER, imp }` pinné, **pas de refresh cookie/ligne**, mutateurs 403 sauf `@AllowImpersonationMutation`. Audit `impersonate.start/end`.
- **SupportCode** : `codeHash` = HMAC-SHA256(pepper ?? ENCRYPTION_KEY, code) hex ; un seul actif ; TTL 60 min (5..1440) ; lockout 5 essais ; comparaison timing-safe. Routes client `POST/GET/DELETE /api/client/support-code` + `POST /api/support/access` (L2+). Audit `support.code.generate/email/redeem/locked`.
- **MFA** : `otplib` (stub Jest `test-utils/otplib.stub.ts`), challenge store mémoire 300 s single-use, méthodes TOTP + email (si mail activé), lockout 5, throttle IP. `mfaRequiredForAdmins` → ADMIN sans MFA reçoit `enrollToken` (setup/confirm seulement). Audit `mfa.enable/verify/disable`.
- **OAuth Google/GitHub** : fetch natif, `OAuthProviderClient` mockable, cookie état `ihp_oauth_state` 10 min, **redirect_uri public (jamais :3001)**. Scénarios callback : login / inscription commande (intent requis) / liaison (conflit 409). `githubTokenEnc` alimente la Phase 10bis. Audit `oauth.login/link/register`.
- **Inscription à la commande** : catalogue public `GET /api/public/products` (visiteur) ; `POST /api/checkout/intent { productId }` → cookie `ihp_checkout` 10 min ; `POST /api/auth/register` **exige l'intent** (403 sinon) → compte + souscription PENDING en `$transaction`. Page `/offres`.
- **Tickets** : `Ticket` + `TicketMessage` + `TicketStatus`/`TicketPriority`, escalade L2/L3, propriétaire ou support ≥ L1. Page `/manager/support`.
- **Turnstile** (siteverify fetch, skip si désactivé/sans clé) sur login + support/access ; **rate limiter maison** (mémoire par IP, 429) sur login/mfa/register/support/checkout.
- 10 migrations au total, `prisma migrate status` up to date ; Prisma client 6.19.3. Stub otplib via `moduleNameMapper` dans les 2 configs jest.

## State — Phase 11 (clés Turnstile admin + base de connaissance + refonte design/UX — migrations 11 `20260902070000_add_turnstile_keys` + 12 `20260902080000_init_knowledge`)
- **Turnstile clés admin** : `SecuritySetting` + `turnstileSiteKey String?` (texte, publique) + `turnstileSecretEnc String?` (AES-256-GCM). `FLAG_KEYS`/`CREATE_DATA` split. `PUT /api/admin/security` sauvegarde les deux, GET ne renvoie **jamais** le secret (`turnstileHasSecretKey`). `''` efface les deux → fallback env. `TurnstileService.isConfiguredAsync()` **DB → env**. `GET /api/public/auth-config` sert la **site key** au widget. UI `/manager/securite` : panneau « Clés Turnstile » (site key + secret « vide = inchangé » / Effacer) + badges.
- **Base de connaissance** : modèle `KnowledgeArticle` + enums `KnowledgeAudience`/`KnowledgeType`/`KnowledgeStatus`, `@@unique([audience, slug])`. Admin `GET/POST/PUT/DELETE /api/knowledge` (ADMIN, audit `knowledge.*`, slug auto + collisions `-2`/`-3`). Client `GET /api/client/knowledge` (uniquement `CLIENT + PUBLISHED`, liste sans `body`), `.../categories`, `.../:idOrSlug` — brouillon client / article admin jamais exposés. UI `/manager/connaissance` (onglets Admin/Client, recherche, filtre statut, Publier/Dépublier, éditeur drawer) + `/aide` (héro + recherche + chips + cartes groupées + lecteur drawer HTML **sanitizé**).
- **Refonte design & conversion (ADR-023 étendu)** : landing `/` (héro + vitrine produit `landing-showcase` + 6 cartes fonctionnalités + bandeau stats + CTA) ; `/offres` catalogue pricing (cartes par type + badge Disponible + Commander + réassurance) ; `/auth` split (aside valeur de marque → carte auth, repli < 880 px) ; shell mobile : hamburger animé + tiroir `mobile-nav` (drawer + overlay + verrouillage scroll + fermeture sur navigation) < 900 px ; topbar glass `backdrop-filter` ; boutons primary gradient + glow + press spring (`--ease-spring`) ; hover-lift panneaux/stat-cards (`--shadow-soft`/`--shadow-lift`) ; fonds ambiants `--bg-glow-1/2` + `background-attachment: fixed` ; `--radius-hero` 18 px ; animation d'entrée `.main`. `brand.ts` `home` → `/`. Tokens `--brand-*` + `config/brand.ts` seuls points de marque (pas de rebrand).
- 12 migrations au total, `prisma migrate status` up to date ; Prisma client 6.19.3.

## State — Phase 10bis (déploiement GitHub → Coolify — migration 13 `20260903033537_init_deployment`)
- **Modèle** : enum `DeploymentStatus {PENDING DEPLOYING ACTIVE FAILED}` + modèle `Deployment` (userId FK Cascade, serviceId FK Cascade, serverId nullable FK SetNull, `repoFullName`, `branch @default("main")`, `coolifyUuid?`, `detail?`, indexes userId/serviceId/status) + back-relations User/Server/Service. **13 migrations**, `prisma migrate status` up to date ; client 6.19.3.
- **`GithubService`** (auto-détection) : `decryptToken` (CryptoService), `listRepos` (`GET https://api.github.com/user/repos?per_page=100&sort=updated`, Bearer + `X-GitHub-Api-Version: 2022-11-28` → `{fullName, defaultBranch, private, language}`), `fetchUser` (login), `repoExists`. Routes client `GET /api/client/github/repos` + `POST /api/client/github/link-status`.
- **`PanelTransport` étendu (COOLIFY)** : `createGitApp` `POST /applications/public` (body `project_uuid:'0'`, `server_uuid:'0'`, `environment_name:'production'`, `git_repository`, `git_branch`, `name`, `build_pack:'nixpacks'` — **endpoint CONFIRMÉ live Coolify 4.1.2**, `/applications/git` → 404), `deployApp` `POST /applications/{uuid}/deploy`, `deploymentStatus` `GET /applications/{uuid}` (best-effort, ne rejette jamais). Hestia → refus.
- **Flow déploiement** (`POST /api/client/deployments`) : gardes `deployEnabled` (403) / GitHub lié / repo possédé / Service ACTIVE / serveur `panelProvider=COOLIFY` + `panelOk=true` → PENDING → createGitApp → deployApp → DEPLOYING (audit `deploy.create`) ; échec → FAILED + audit `deploy.failed` + 502 ; `GET /api/client/deployments` (les siens) ; `GET .../:id` **rafraîchit live** quand DEPLOYING (statut illisible / Coolify injoignable → état courant conservé) + audit `deploy.status`. **`coolifyUuid` jamais renvoyé** (`toView`).
- **Web `/client` — panneau « Déploiements GitHub → Coolify »** : conditionné à `deployEnabled` (servi par `GET /api/public/auth-config`), liaison GitHub (lien `/profil` si non lié), select repo (branche par défaut), select service ACTIVE, Déployer/Actualiser, badges PENDING/DEPLOYING/ACTIVE/FAILED, **poll 8 s** tant que DEPLOYING.
- **Exigence opérationnelle (constatée en live)** : Coolify exige un **jeton API ROOT / write-scope** pour créer une application (jeton lecture seule → 403 « Missing required permissions: write ») — comportement attendu, consigné ADR-030. Repos PRIVÉS via GitHub App Coolify = follow-up documenté.

## State — Phase 3 Cloudflare DNS (ADR-033 — migrations `20260905001000_add_cloudflare_dns` + `20260906010000_rename_pack_disk_to_storage_limit`)
- **Contrôle DNS Cloudflare admin** (`/manager/dns`) : module `cloudflare` — zones racines **importables**, choix du domaine racine, **enregistrements DNS live (proxied)**, allocation sous-domaine client. Clé API **AES-256-GCM chiffrée, jamais renvoyée** (`CloudflareSetting`). Modèles `Domain` (`DomainStatus`) + `ClientSubdomain` (`fqdn` **unique**, `SubdomainStatus`).
- **Sous-domaine client alloué automatiquement au déploiement** : `Deployment` + `subdomain`/`domainId`/`fqdn`/`clientSubdomain` — CNAME → hostname Coolify ; **`fqdn`** (ex `monapp.arumdigital.com`) = URL reachable exposée au client. La **purge app Phase 13** (`52cae27`) supprime le sous-domaine Cloudflare.
- **Quota disque clean-up** : machinerie `--storage-opt`/`mergeStorageOpt` **supprimée** ; `HostingPack.diskGb` → **`storageLimit`** (valeur préservée), aligné `Plan.cpu_limit/memory_limit/storage_limit`. **Enregistré + affiché (admin + offres) mais quota PAS actif** (branché après mise en prod).
- Vérifié **en LIVE (clé réelle)** : zones validées, CNAME créer/lister/supprimer, allocation OK.

## State — Phase 12 (ADR-031, catalogue Produit → Catégorie + Pack avec limites Coolify — migration additive)
- Modèles `ProductCategory` + `HostingPack` (`ramMb`, `cpuCores`, `storageLimit`, `maxApps` Phase 13) ; `Product` + `categoryId?`/`packId?`. Modules backend `categories` + `packs` : **CRUD admin** (audité, **suppression protégée** si référencé — 409, pattern produits), **recommandation de pack**.
- **Limites réellement appliquées à Coolify** : `applyPack` → **`PATCH /applications/{uuid}`** (RAM/CPU du pack) **AVANT** `deployApp` best-effort. **`deployApp` → `POST /applications/{uuid}/deploy`** (vérifié live Coolify 4.1.2 ; `/applications/{uuid}/deploy` = 404).
- **Serveur Coolify cibles configurables** : `Server.coolifyProjectUuid`/`coolifyServerUuid` (le `'0'` par défaut n'existe pas sur 4.1.2 → l'admin choisit projet/serveur réels).
- Web : `/manager/packs` + `/manager/categories`, produits enrichis (catégorie+pack), `/offres` + espace client (limites), `/manager/serveurs` (uuid Coolify). Vérifié : **297 unit (31 suites) + 149 e2e** + tsc API/web + build + **validation réelle Coolify** (app « Test limits pack » `limits_cpus=1`/`limits_memory=1g`).

## State — Phase 13 (ADR-032 « ADR Module », modules de déploiement A/B + projet client + monitoring + dashboard client)
- **`DeploymentModule`** + enum `DeploymentModuleKind {SHARED_PROJECT | PER_CLIENT_PROJECT}` (migrations `20260906020000_add_deployment_modules_and_client_projects` + `20260907010000_fix_deployment_module_kind_enum`) : `name`/`code` uniques, `isActive`, `serverId` (COOLIFY connecté), `sharedProjectUuid/Name` (**Module A = projet partagé**, choisi par l'admin via liste live), `perClientPrefix` `client` (**Module B = projet client dédié**), overrides `overrideRamMb/overrideCpuCores/overrideStorageLimit`. Lié aux `HostingPack` (`maxApps` = **quota d'apps**, N/M affiché — le client ne dépasse pas son quota).
- **`ClientProject`** (Module B, `@@unique([userId,serverId,moduleId])`) : `name` « client-<uid> », `projectUuid` ; créé **paresseusement** à la première app ou via `POST /api/admin/users/:id/project` (page Utilisateurs).
- **Client déploie sans Service** (migration `20260906030000_make_deployment_service_optional`) : `Deployment.serviceId` optionnel — cible résolue depuis le **pack ACTIF → module A/B** ; `Deployment` + `coolifyProjectUuid`/`moduleId`/`clientProjectId` (tracé monitoring/support).
- **PanelTransport Coolify projets** : `createProject`/`listProjects` (fix `{success}` sans tableau — `52cae27`), `applyPack`, `deployApp` **`POST /deploy`** (vérifié live). **Monitoring** : `GET /api/admin/monitoring/projects` + page `/manager/monitoring` (barres ressources, badges danger, tri consommation).
- **Web `/client` refonte** : hero + stats + cartes apps **quota N/M**, **suppression d'app 2 étapes** (`DELETE /api/client/deployments/:id` purge app Coolify + **sous-domaine Cloudflare DNS**, quota libéré), **upgrade pack** (`PATCH /api/client/subscriptions/:id/upgrade`, même ligne, données conservées), lien `/profil`. **Auth** : fenêtre de réutilisation **10 s** sur refresh. +5 articles admin connaissance. `start.sh`.
- Invariants : `coolifyUuid`/jetons **jamais exposés** ; `deployEnabled` reste la garde (OFF par défaut). Vérifié vert à chaque sous-phase (unit/e2e/tsc/build) ; validation réelle Coolify (« Test limits pack »).

## State — Phase 14 (ADR-034, plateforme multi-brand / white-label — migrations `20260908010000_add_brand_config` + `20260908020000_add_brand_logo_show_text`, **25 migrations**)
- **`BrandConfig` singleton** (id fixe `'brand'`) : `name`, `sub`, `tagline?`, `hostname?`, `logoType` (enum `BrandLogoType {DEFAULT TEXT IMAGE}`), `logoText?`, `logoUrl?`, **`logoShowText Boolean @default(false)`**, `primaryColor` (`#00b377` défaut), `accentColor?`, `updatedById?`. Ligne **garantie** par `onModuleInit` (idempotent, aucun seed).
- **API** : `GET /api/brand` (**public**) · `PATCH /api/admin/branding` (ADMIN, hex validé, audit `branding.update`) · `POST reset` (défauts, fichier supprimé, audit `branding.reset`) · `POST logo` (multipart PNG/JPEG/WebP ≤ 2 Mo, **SVG refusé XSS**, audit `branding.logo`) · **`POST logo/remove`** (fichier supprimé, retour DEFAULT, audit `branding.logo-remove`). Assets servis `@Controller('branding')` → **`/api/branding/:file`** (fix ORB : `logoUrl` stocké sous `/api/branding/`, le chemin `/branding/` 404 → `ERR_BLOCKED_BY_ORB`).
- **Logo IMAGE — image seule** (`logoShowText=false`) : masque nom/sous-titre/tag dans topbar/sidebar (`app-shell` masque `brand-col`) ; `logoShowText=true` → image + wordmark (fallback nom). **Nom de marque FACULTATIF** (une marque « image seule » sans nom ; sous-titre/tag se vident correctement).
- **Application sans flash** : `generateMetadata()` + `<style :root>` injecté dans `layout.tsx` (`--brand-*` en `!important` → recolor badges/active/glows/globe/touches) ; `BrandProvider`/`BrandLogo` ; `globals.css` dérive les verts via **`color-mix(var(--brand-primary))`** ; `config/brand.ts` = source des défauts (`defaultBrand`). Article « Configurer la marque » seedé (idempotent). Page admin `/manager/apparence`.
- **Vérifié** : unit branding **15 tests verts** ; test réel navigateur (Chrome + cookies) — **IMAGE seule → topbar `imgCount:1`/`brandTextNodes:0` (texte masqué)**, IMAGE+texte → wordmark, image `naturalWidth>0` zéro ORB ; test réel replay : nom Code Diali conservé **sans toucher le vert `#00b377`** (demande propriétaire).

## State — Phase 16 (ADR-035 order-driven + ADR-036 build config fichier — `539cacd` → `4705366` → `11d3592`, **34 migrations**)
- **Abonnements par commande (ADR-035, Blocs 1–7)** : la table **`Service` est supprimée** (migrations `20260911150000_drop_service`) ; une souscription est liée à **produit + pack + commande** (`20260911140000_order_subscription_relation`, `20260911085322_order_requested_subdomain`). Les abonnements sont créés **`ACTIVE` par la procédure de commande** (checkout store — le paiement vaut approbation ; `PENDING→approbation` disparaît) ; l'admin garde suspendre/réactiver ; le client annule. `checkout.store` (retry idempotent par `idempotencyKey`, `20260909110000_add_order_idempotency_key`) produit la ligne ACTIVE.
- **Plan gratuit sans checkout** : produit `$0` + pack **Starter** (`maxApps:1`, 256 Mo/0.5 CPU) lié au **module de déploiement B** (`PER_CLIENT_PROJECT`, préféré car le projet partagé A peut être hors d'accès du token — commenté seed-store) ; `POST /api/auth/free-signup` (`FreeSignupDto` étend `RegisterDto` + `planSlug?`) et OAuth **`mode=free`** ; quota d'apps N/M servi depuis `HostingPack.maxApps` ; validation Bloc 3 (build lourd sous petit pack échoue l'APP seule, serveur reste opérationnel).
- **Unification store→Deployment** (migration `20260911160000_free_signup_file_build`) : la voie client déploie via `Deployment` unifié ; `probe-container.js` aidé ; 4 specs e2e migrées sur le modèle order-driven (ADR-035).
- **Build config fichier (ADR-036, Phase 16b `539cacd`)** : un repo peut porter **`codediali.toml` (prioritaire) ou `netlify.toml`** → `BuildConfig` pré-rempli (`previewBuildConfig` source `codediali.toml` > `netlify.toml` > `none`), appliqué à la création. **Détection SPA Vite** : `detectViteSpa` → `isStatic:true` + `publishDirectory:'/dist'` ; à la création **nixpacks + `is_static:true` + `publish_directory:/dist`** (NE PAS forcer `build_pack:'static'` → sert l'`index.html` SOURCE, page vide HTTP 200). Exigence Coolify : `publish_directory` AVEC slash de tête (`/dist`, sinon 422).
- **Mode URL collé (Phase 10bis.5 + fix 16)** : `POST /api/client/deployments/detect {url}` (SSRF-guardé) ; `create` en mode URL **saute** GitHub token/ownership. **Fix réel 2026-09-13** (`apps/web/src/app/client/project/page.tsx`) : `deploy()` ne doit envoyer **`repoUrl`** que si `detected.repoUrl` (lien collé), jamais `repoFullName` figé — sinon le serveur bascule en mode GitHub lié → `decryptToken` → « Aucun compte GitHub lié ». Un client peut déployer un dépôt public **sans compte GitHub lié**.
- **Détection du SPA** : juge le **serveur réel** (scripts `start/serve/server/prod` exécutant `node|tsx|ts-node|nodemon|pm2|next|nest`), pas la seule présence d'une dep ; un `express` résiduel ne fait pas un back-end ; `source:'none'` = auto.

## State — 2026-09-13 — validation réelle déploiement dépôt public (mode URL), exécutée en autonomie
- **Scénario validé** : compte gratuit → `POST /api/client/deployments {repoUrl:'https://github.com/merrabii/Code-Diali-Guide-de-Demarrage'}`
  → **module B** projet client créé sur Coolify → `createGitApp` **nixpacks** `is_static` `publish_directory:/dist` → limites pack appliquées (256 m / 0.5 CPU)
  → build ~107 s → **`ACTIVE`** → `https://code-diali-guide-de-demarrage.arumdigital.com` **HTTP 200** (SPA réel : titre « Code Diali Cloud - Guide de Démarrage »).
  App ensuite **supprimée** par le chemin de code (`DELETE /api/client/deployments/:id`) → **quota libéré**.
- **Cause racine du 403 « You are not allowed to access the API » (2006-09-13, corrigé par le propriétaire)** : ce n'était **PAS** un jeton non-ROOT — le jeton est **ROOT et valide** ; c'était la **liste blanche d'IP de l'API Coolify** (`portal.arumdigital.com:8000`). Une fois l'IP autorisée dans Coolify, `POST /projects` (module B) **et** `POST /applications/public` (module A) fonctionnent aussitôt. **À relire par toute session/AI future** : ne PAS conclure « token en scope insuffisant » sur ce message ; vérifier l'IP autorisée côté Coolify d'abord (cf. PIP dynamique aussi pour Brevo, Phase 6).
- Reste vrai : c'est **le chemin de code/API panel** qui déploie (jamais l'UI Coolify ni Docker manuel — architecture [[architecture]]).

## State — Audit & corrections techniques Phase 11 (2026-09-03, directive propriétaire : audit → corrections uniquement → matrice → trafic/quotas + ADR → DESIGN_HANDOVER → tests → rapport final ; NE PAS commiter / pousser / faire 10bis)
- **Corrections techniques (3, aucune feature neuve)** :
  1. **Delete produit référencé → 409** : `products.service.remove` compte `subscription` avant suppression physique (`ConflictException` « Ce produit est référencé par des souscriptions — passez-le en statut DISABLED pour le retirer du catalogue. ») + spec unit. L'admin garde le contrôle : DISABLED retire du catalogue sans supprimer l'historique.
  2. **Espace client sans DRAFT/DISABLED** : `/client` lit `listPublicProducts()` (le catalogue client = catalogue public, `GET /api/products` reste authentifié — core.e2e intact).
  3. **Texte éditeur connaissance** aligné sur la réalité (`/aide` public).
- **Base de connaissance seedée** : `apps/api/prisma/seed-knowledge.ts` (idempotent create-if-missing par `[audience, slug]`, ne modifie jamais les edits admin, auteur = plus ancien ADMIN, tout PUBLISHED) + script `db:seed:knowledge`. **Validé sur la DB réelle `icode-postgres`** : 34 créés (23 ADMIN : 12 INFORMATIVE récap + 4 TECHNICAL + 7 HOWTO ; 11 CLIENT : Premiers pas / Compte & sécurité / Support) ; re-run → 0 créé / 34 existants (non modifiés).
- **Docs créées** : `docs/permissions-matrix.md` (matrice route→accès réelle + séparation stricte connaissance + règles transversales) · `docs/traffic-quotas.md` (analyse 4 couches mesure/quota/limitation/suspension — `quotaMaxAccounts` NON enforce, suspension manuelle `isActive` ; **ADR-029 PROPOSED**, rien d'implémenté) · `docs/parcours-commande-publique.md` (audit bout en bout + écarts documentés sans invention) · `docs/design/DESIGN_HANDOVER.md` (point d'entrée du futur agent design — 8 règles, carte fichiers, périmètre hors-design).
- **Correctif isolation e2e (cause racine, non-régression)** : 9 échecs pré-existants (`security-settings` 2 + `oauth` 7) dus au **clobbering inter-suites** (Postgres partagé + singleton `SecuritySetting` + workers jest parallèles) — prouvé `--runInBand` (chaque suite passe seule). Fix : `"maxWorkers": 1` dans `apps/api/test/jest-e2e.json`.

## Verified (audit & corrections — real checks, 2026-09-03)
- Unit **224/224 (25 suites)** PASS (+1 spec products 409).
- e2e **132/132, 18 suites** PASS sur Postgres réel (après `maxWorkers: 1` — correction de l'isolation inter-suites ; non-régression intégrale des 18 suites).
- `npx tsc --noEmit` apps/api **PASS** ; apps/web **PASS**. `web build` **PASS** (18 routes ; dev arrêté + `.next` purgé avant build). Seed connaissance : 34 créés puis idempotence prouvée (0 créé au re-run).

## Verified (Phase 6 — real checks, 2026-08-31)
- Unit **90/90** (11 suites : + crypto 5, mail 5, **mail-settings 14**, invitations 14 — mocks MailSettingsService).
- e2e **61/61, 8 suites** (+ **mail** 10 tests avec `overrideProvider(MailTransportFactory)` : 401/403 RBAC, defaults masqués, PUT store → GET hasPassword jamais raw, PUT enabled sans host 400, test OK/400 message SMTP, invite email enlevé→true / désactivé→false, audit masqué). Aucun SMTP réel contacté.
- Builds API + web PASS (route `/manager/mail` incluse). Typecheck web PASS. `prisma migrate status` → in sync.
- Live smoke API :3001: admin login → `GET /api/admin/mail` defaults masqués `{host:null,hasPassword:false}` → `POST /api/admin/mail/test` sans config → 400 « Configuration mail non définie. ».

## Owner live validation (Phase 6 — 2026-08-31)
- **VALIDÉ par le propriétaire** : domaine **codediali.com** acheté et lié/validé dans Brevo ; config SMTP Brevo réelle enregistrée (host `smtp-relay.brevo.com`, port 587, user `9bda29001@smtp-brevo.com`, fromEmail **contact@codediali.com**, fromName « iCode Host Pro », enabled) ; mail de test envoyé et **`delivered` confirmé dans Brevo Logs→SMTP** (vers mourad.moreno@gmail.com + mourad.errabii@gmail.com). Password jamais réaffiché (`hasPassword` only, audit masqué).
- Incidents Brevo maîtrisés pendant la validation (côté compte Brevo, remontés correctement par l'app) : **525 Unauthorized IP** (autoriser l'IP publique dans Brevo — IP ADSL **dynamique**, à réautoriser au changement) ; **sender non validé** → rejet ASYNCHRONE post-250 (l'API montrait `ok:true` ; vu en Brevo Logs→SMTP event `error` « sender not valid ») — fix avec expéditeur validé `contact@codediali.com`.
- **Rebrand (note owner, différé)** : « iCode Host Pro » → **Code Diali** (`codediali.com`) une fois le projet terminé.

## State — Phase 8 (ADR-025, connexion réelle serveurs)
- **`Server` +3 champs de résultat** (migration `20260901082020_init_server_check`, **7 migrations**) : `lastCheckedAt DateTime?`, `lastProbeOk Boolean?` (null = jamais sondé), `lastProbeDetail String?` — **nullable, écrits uniquement par la sonde** (jamais par l'admin ; whitelist create/patch inchangée).
- **`ProbeTransportFactory`** (`src/servers/probe-transport.factory.ts`) — couture de test (modèle `MailTransportFactory`) : `ServersService` dépend de la factory ; e2e override, unit teste le vrai transport sur loopback. Runtime `NodeProbeTransport` : **TCP** (ports type SSH, défaut 22) + **HTTP/HTTPS** (80/443/8443, `rejectUnauthorized = strictTls`) ; toute réponse HTTP = joignable ; timeout défaut 5 000 ms ; détail lisible. **Leçon DI Nest** : aucune primitive au constructeur (le timeout vit dans `create(timeoutMs)`).
- **Endpoint** `POST /api/servers/:id/check` (ADMIN) : persiste les 3 champs + audit `server.check` ; **le statut n'est JAMAIS forcé** — la sonde `→ ACTIVE`/`→ PROBLEM` n'est qu'un raccourci admin (PATCH + audit `server.update`).
- **UI** `/manager/serveurs` : colonne **Connexion** (badge OK/Échec/— + détail), bouton **Tester**, bascule rapide.
- **Rework UX (Phase 8 bis, commentaire propriétaire — grille de cartes + drawer, ADR-023)** : la table 11 colonnes à édition inline est remplacée par une **grille de cartes** moderne — toolbar **recherche** (nom/hostname/IP/fournisseur/région) + **chips filtres par statut** avec compteurs (mémoire, zéro appel API), cartes responsives (en-tête icône teintée par statut + nom + badge statut + hostname mono ; champs IP/Port/Quota mono + Fournisseur/Région + badges TLS/Panneau ; **bloc Connexion Phase 8 intégré** : badge OK/Échec/— + détail + « Dernier test » + bouton Tester + bascule rapide ; pied Modifier/Supprimer, actions révélées au survol). **Édition/création dans un panneau latéral (drawer)** (animation slide + overlay blur, Échap/clic backdrop ferme, sélect statut en édition seulement). Icônes `IconSearch`/`IconPencil`/`IconTrash`. Pattern `srv-*` + `drawer-*` réutilisable (Produits…). **Zéro changement API/DB.**

## Verified (Phase 7ter — real checks, 2026-09-01)
- Unit **91/91** (11 suites, +1 full-details serveur) ; **e2e 62/62, 8 suites** (+1 ADR-024 : POST serveur avec ip/port/provider/region/quota/strictTls/panelProvider → 201, PATCH panelProvider+port → 200) — verts sur Postgres réel.
- `npx tsc --noEmit` apps/web **PASS** ; `web build` **PASS** (14 routes dont `/manager/serveurs` 5.28 kB, `/manager/produits` 3.80 kB ; `.next` purgé avant build). `prisma migrate status` → 6 migrations in sync.
- Smoke live :3000 → **200** sur `/manager`, `/manager/serveurs`, `/manager/produits` + proxy `/api` 401 sans session. Login ADMIN → `/users/me` 200 ; `/api/servers` (4), `/api/products` (2), `/api/manager/summary` (`{products:2 ACTIVE, servers:4, users:16/17}`) cohérents ; zéro erreur côté web/API.
- **Redémarrage environnement** : Docker Desktop était éteint (postgres au sol, dev servers arrêtés — le web « pas accessible »). Remonté : docker up (postgres healthy), API :3001, web :3000. État d'environnement, pas une régression de code.
- Validation live propriétaire en cours (le propriétaire a créé un serveur `momo|10.10.2.36` + un produit `Installation Fees` via l'UI entre deux smoke).

## Verified (Phase 8 — real checks, 2026-09-01)
- Unit **98/98** (12 suites, +7 : 3 check du service — succès/défaut port 22, échec port explicite, 404 — + 4 transport réel sur loopback : HTTP 200, TCP ok, refusée, hôte introuvable).
- **e2e 67/67, 9 suites** (+ `server-check` 5 tests, `overrideProvider(ProbeTransportFactory)` → **zéro réseau réel en test**) PASS sur Postgres réel.
- `npx tsc --noEmit` apps/web **PASS** ; `web build` **PASS** (14 routes, `/manager/serveurs` 5.8 kB ; `.next` purgé avant build). `prisma migrate status` → **7 migrations in sync**.
- Smoke live :3001 — `POST /api/servers/:id/check` réel : `probe-smoke-local` (localhost:5432) → `{ok:true, detail:"TCP 5432 : accessible (8 ms)", latencyMs:8}`, persisté + relu en GET ; `probe-smoke-refused` (127.0.0.1:5999) → `{ok:false, detail:"Connexion refusée"}`, persisté ; audit `server.check` `{ok, host, port, detail, latencyMs, httpStatus, statusLeft}`. Smoke :3000 → **200** `/`, `/manager/serveurs`, `/manager/journal` ; label « Test de connexion serveur » dans le bundle.
- Dev servers relancés en fond : API :3001 (`nest watch`) + web :3000 (`next dev`) pour validation propriétaire.
- **Rework UX (Phase 8 bis)** : `npx tsc --noEmit` apps/web **PASS** (exit 0) ; `web build` **PASS** (14 routes, `/manager/serveurs` 6.39 kB ; `.next` purgé + dev web arrêté avant build) ; marqueurs `srv-grid`/`srv-card`/`drawer-overlay`/`srv-chip`/`IconSearch`/`IconPencil`/`IconTrash` dans le chunk client compilé + CSS servi ; `/manager/serveurs` **200**. Aucun endpoint/garde touché → unit 98/98 + e2e 67/67 toujours valides.

## Verified (Phase 7 polish UI — real checks, 2026-08-31)
- `npx tsc --noEmit` dans `apps/web` → **PASS (exit 0)**.
- `web build` → **PASS** (10 routes intactes ; dev web arrêté + purge `.next` avant build — leçon Phase 2), puis `next dev` relancé sur :3000.
- Smoke HTTP :3000 → **200** sur les 9 pages (`/`, `/auth`, `/client`, `/manager`, `/manager/invitations`, `/manager/journal`, `/manager/mail`, `/manager/subscriptions`, `/manager/utilisateurs`).
- **Aucun changement API/DB** : pas de migration, suites API inchangées (unit 90/90 + e2e 61/61).

## Verified (Phase 7 — real checks, 2026-08-31)
- `npx tsc --noEmit` dans `apps/web` → **PASS (exit 0)**.
- `corepack pnpm --filter @codediali/web build` → **PASS** (10 routes, exit 0). Dev web arrêté pendant le build (risque de corruption `.next` — pratique établie Phase 2) ; API :3001 restée up.
- Smoke HTTP :3000 → **200** sur `/`, `/auth`, `/manager`, `/manager/utilisateurs`, `/manager/journal`, `/manager/invitations`, `/manager/mail`, `/manager/subscriptions`, `/client`. HTML servi : `lang="fr"`, script `ihp-theme` (anti-FOUC) présent ; CSS servi contient les tokens du design system (29 Ko, marque `#00b377`, fonds dark/light).
- **Aucun changement API/DB** : pas de migration, aucun test API touché (unit 90/90 + e2e 61/61 inchangés depuis la Phase 6).

## Pending
- **(Optionnel, Phase 6) test live d'invitation avec email** : créer une invite → l'email arrive avec le lien `/auth?invite=…` → accept de bout en bout.
- **Phase 14 ✓ VALIDÉE 2026-09-08 (2 fois par le propriétaire), commitée (`2cf7b1d` + `8e5abcf`) + poussée.** Phases 12 (`1d2527d`), 13 (`0bfd856`→`52cae27`) et Cloudflare DNS (`5395840`) **également poussées** — `origin/main` à jour.
- **Quota disque** : le `storageLimit` des packs est **enregistré + affiché mais INACTIF** (décision « branché après mise en prod »). Le quota **d'apps** (`maxApps`, Phase 13) est, lui, **actif**.
- **Phase 16 ✓ validée réel (2026-09-13)** : déploiement URL public → app SPA live HTTP 200 (jeton ROOT + IP autorisée sur Coolify). `deployEnabled` reste **ON** sur le SecuritySetting local ; le panneau « Créer un nouveau Projet » est disponible. Le `Service` n'existe plus (supprimé ADR-035).
- **Test unit flaky `invitations.service.spec` (2026-09-03)** : 1 échec non reproductible en isolé (16/16), `expiresAt = Date.now()+1 s` fragile en run long — suivi/durcissement optionnel avant tout futur commit.
- **Rebrand iCode Host Pro → Code Diali (`codediali.com`) : DIFFÉRÉ à la fin du projet** (décision propriétaire 2026-08-31, règle conservée) — modifier `config/brand.ts` + tokens `--brand-*` uniquement, sans toucher au vert `#00b377`.
- **ADR-029 conservé PROPOSED (décision propriétaire 2026-09-03)** : si validé plus tard, enforcement des quotas (`Server.quotaMaxAccounts` → blocage des nouvelles souscriptions/services au-delà) + éventuelle politique de suspension automatique (aujourd'hui manuelle via `User.isActive`).
- Priorités ouvertes proposées ensuite : politique automatique des statuts serveur (sonde périodique, ADR-007/010), cPanel/DirectAdmin, observabilité, ADR-008 complet (gestion de secrets / config persistée), billing/paiements.
- Le propriétaire a signalé **« J'ai d'autres modifications et options à ajouter »** — la gouvernance étant rattrapée, la prochaine phase est définie par ces demandes (pas de Phase 15 prédéfinie).

## Decisions
- ADR-001..005, 011..014: **APPROVED** (Phase 0). ADR-015+016: **APPROVED** (Phase 1). ADR-017: **APPROVED** (Phase 2). ADR-018: **APPROVED** (Phase 3). ADR-019: **APPROVED** (Phase 4). ADR-020+021: **APPROVED** (Phase 5 GO). **ADR-022 (configuration mail + emails d'invitation) : APPROVED** (Phase 6 GO, 2026-08-31) — valide un périmètre étroit d'ADR-008 (chiffrement applicatif au repos). **ADR-023 (design system de l'interface) : APPROVED** (Phase 7 GO, 2026-08-31) + polish UI du même ADR. **ADR-024 (détails infrastructure Serveurs + pages CRUD admin larges, Phase 7ter) : APPROVED** (2026-09-01). **ADR-025 (sonde de connectivité réelle des serveurs, Phase 8) : APPROVED** (2026-09-01). **ADR-026 (métriques serveur + auto-détection IP/port + accès direct, Phase 9bis) : IMPLEMENTED 2026-09-02**. **ADR-027 (sécurité, comptes & support — Phase 10) : APPROVED**. **ADR-028 (base de connaissance + clés Turnstile admin — Phase 11) : IMPLEMENTED 2026-09-02** ; extension design/UX = ADR-023. **ADR-029 (trafic, quotas & suspension — 4 couches) : PROPOSED 2026-09-03 — rien d'implémenté** (volontairement PAS dans la liste APPROVED). **ADR-030 (déploiement GitHub → Coolify — Phase 10bis) : IMPLEMENTED 2026-09-03** (createGitApp `/applications/public` confirmé live, `Deployment` coolifyUuid jamais exposé, jeton API ROOT requis) + **ADDENDUM 10bis.5 (URL collée + détection auto)**. **ADR-031 (catalogue Produit → Catégorie + Pack avec limites Coolify — Phase 12) : IMPLEMENTED 2026-09-05**. **ADR-032 (modules de déploiement A/B + projet client + monitoring — Phase 13) : IMPLEMENTED 2026-09-06/07**. **ADR-033 (contrôle DNS Cloudflare admin + sous-domaine client + quota disque clean-up) : IMPLEMENTED 2026-09-05**. **ADR-034 (plateforme multi-brand / white-label — Phase 14) : IMPLEMENTED 2026-09-08, validé 2× par le propriétaire**. **ADR-035 (abonnements par commande, suppression Service, limites strictes, plan gratuit — Phase 16) : IMPLEMENTED 2026-09-11 (+ validation réelle 2026-09-13)**. **ADR-036 (build config fichier `codediali.toml`/`netlify.toml` + détection SPA Vite statique — Phase 16b) : IMPLEMENTED 2026-09-09/11**. ADR-006 full, 007, 008 complet, 009: **PROPOSED** (untouched). See DECISIONS.md.

## Next action
**Phase 16 validée réel (2026-09-13) et poussée.** Dépôt public (mode URL) → app SPA live **HTTP 200** confirmé de bout en bout en autonomie ; app de test supprimée (quota libéré). **Configuration opérationnelle en vigueur en local** : `deployEnabled=true`, jeton Coolify **ROOT** + **IP autorisée** sur `portal.arumdigital.com`. Prochaine étape : le propriétaire définit la **Phase 17** — plusieurs demandes métier en attente d'arbitrage (déploiement depuis un **repo GitHub lié** non encore validé de bout en bout, politique de quota disque `storageLimit`, rebrand Code Diali différé fin de projet, test install plateforme repoussé fin de projet). Rappel : aucun push sans OK explicite ; ops apps par le chemin de code/API panel. Travaux ouverts si demandés : brancher le quota disque (`storageLimit`) après mise en prod, durcir eventual flaky `invitations.service.spec`.

## Out of scope (do not build yet)
Billing/paiements, jobs/Redis (ADR-007), politique automatique des statuts serveur (sonde périodique), cPanel/DirectAdmin, asset storage, reverse proxy/SSL, observability, ADR-008 complet (gestion de secrets / config persistée), quota disque actif (`storageLimit`), rebrand Code Diali (différé à la fin du projet), ADR-029 (quotas/suspension appliqués).
